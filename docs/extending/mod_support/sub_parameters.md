---
title: Subparameters
icon: lucide/square-function
---

# Subparameters

Subparameters are multiple parameters bundled into one class instance, that can be used a parameter in a `DensityFunction` type, a `RegistryResource` or another `SubParameter`.